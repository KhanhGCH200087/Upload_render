
        <div class="container-lg">
            <div class="row">
                <div class="col-md-10 mx-auto">
                    <div class="contact-form">
                        <h1>Enter Schedule Detail</h1>
                        {{#if InputErrors}}
                            <div>
                                <p>Please correct the following errors:</p>
                                <ul>
                                    {{#each InputErrors}}
                                        <li>{{this}}</li>
                                    {{/each}}
                                </ul>
                            </div>
                        {{/if}}
                        {{#if message}}
                           <p>{{message}}</p>
                        {{/if}}

                        <form action="" method="post"> 
                            <div class="form-group">
                                <label for="inputSubject">Name</label>
                                <input type="text" class="form-control" name="name" required>
                            </div>
                            <div class="form-group">
                                <label for="inputSubject">Date</label>
                                <input type="date" class="form-control" name="date" required>
                            </div>
                            <div class="row">
                                <div class="col-sm-4">
                                    <div class="form-group">
                                        <label for="inputName">Class</label>
                                        <label>&nbsp;&nbsp;</label>
                                        <select name="classes" id="">
                                            {{#each classList }}
                                            <option value="{{_id}}">
                                                {{ name }}
                                            </option>
                                            {{/each}}
                                        </select>
                                    </div>
                                </div>     
                            </div>      
                            
                            <button type="submit" value="Edit" class="btn btn-primary"><i class="fa fa-paper-plane"></i>Add New</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
  